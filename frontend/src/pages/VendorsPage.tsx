import PlaceholderPage from '../components/PlaceholderPage'
import { VendorsIcon } from '../components/NavIcons'

function VendorsPage() {
  return (
    <PlaceholderPage
      title="Vendors"
      icon={<VendorsIcon />}
      description="Vendor management is coming soon."
    />
  )
}

export default VendorsPage
